global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "eventzella_backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["backend:5000"]

rule_files:
  - "/etc/prometheus/alert_rules.yml"

alerting:
  alertmanagers: []